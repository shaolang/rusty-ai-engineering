[package]
name    = 'vector-db-macro'
version = '0.1.0'
edition = '2024'

[dependencies]
proc-macro2 = '1'
quote       = '1'
syn         = { version = '2', features = ['derive'] }

[lib]
proc-macro = true
